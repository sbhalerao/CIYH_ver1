source 'http://rubygems.org'

gem 'rails', '3.0.0'
gem 'rake','0.8.7'
gem 'aws-s3'								#Amazon-S3 gem to save images
gem 'rmagick', :require => 'RMagick'		#Gem for image manipulation. RMagick = name of library
gem 'twitter'

# Bundle edge Rails instead:
# gem 'rails', :git => 'git://github.com/rails/rails.git'

gem 'sqlite3-ruby', :require => 'sqlite3'
gem 'thumbs_up'												   # vote and karma management
gem 'omniauth', :git => 'git://github.com/soopa/omniauth.git'  # authentication using omniauth

gem 'fb_graph'					# Facebook gem
gem 'will_paginate', '~> 3.0.2' # to enable pagination
gem "high_voltage"				# static pages in RoR app


# Use unicorn as the web server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger
# gem 'ruby-debug'

# Bundle the extra gems:
# gem 'bj'
# gem 'nokogiri'
# gem 'sqlite3-ruby', :require => 'sqlite3'
# gem 'aws-s3', :require => 'aws/s3'

# Bundle gems for the local environment. Make sure to
# put test-only gems in this group so their generators
# and rake tasks are available in development mode:
# group :development, :test do
#   gem 'webrat'
# end
