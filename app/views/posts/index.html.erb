<h1>Listing posts</h1>
<h2><a href="#" onclick="alert('Hello world!'); return false;">Click Here for a Javascript test</a></h2>

<table>
  <tr>
    <th>Title</th>
    <th>Name</th>
    <th>Content</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.title %></td>
    <td><%= post.name %></td>
    <td><%= post.content %></td>
	<td><%= post %></td>
	<td><%= post.id %></td>
    <td><%= link_to 'Show', post %></td>
    <td><%= link_to 'Edit', edit_post_path(post) %></td>
    <td><%= button_to 'Destroy', post, :confirm => 'Are you sure?', :method => :delete %></td>
   <td> <%= link_to 'Message', jmsg_path(:msg => "hello world" , :id => post.id), :remote => true, :pid => post.id, :id => "messageid", :class => "article"%> 
	<div id ='trialid'> Trial </div>
	<div id = 'show_message'></div> </td>

  </tr>
<% end %>
</table>
<%= Post.last.id %>
<br />

<%= link_to 'New Post', new_post_path %>



<%= link_to 'Enter message', fbpost_path %>
